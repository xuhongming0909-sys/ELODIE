# Store local sensitive values only. Do not commit this file.
# Do not place real values in markdown, specs, memory, or missions.
# A single file must stay under 500 lines.
# Templates are reference material, not a requirement to keep every key literally.
# Keep only the sensitive configuration that the current project actually needs.

api:
  key: ""
  secret: ""

accounts:
  username: ""
  password: ""

tokens:
  access_token: ""
  refresh_token: ""

services:
  database_url: ""
  cookie: ""

# Example:
# api:
#   key: "env:OPENAI_API_KEY"
#   secret: "env:OPENAI_API_SECRET"
# accounts:
#   username: "your_account"
#   password: "your_password"
